# 🧰 Setup e Instalación de Entornos Interactivos de Python

Este documento describe los pasos para instalar y configurar dos entornos de desarrollo recomendados para trabajar con Python en modo interactivo y científico:

* [Marimo](https://marimo.io): un entorno moderno para notebooks interactivos.
* [Spyder](https://www.spyder-ide.org/): un entorno científico tradicional, ideal para ingeniería, análisis de datos y programación estructurada.

---

## 🚀 **1. Instalación de Marimo**

### 🧬 ¿Qué es Marimo?

[Marimo](https://marimo.io) es un entorno de notebooks **interactivos y reproducibles**, diseñado como alternativa moderna a Jupyter.
Permite crear celdas reactivas con Python puro, sin depender del estado global del notebook, y exportar tus resultados fácilmente como aplicaciones web.

### 🔧 Requisitos previos

* Tener instalado **Python 3.8 o superior**.
  Puedes verificarlo ejecutando:

  ```bash
  python --version
  ```
* Tener instalado **pip** (gestor de paquetes de Python).
  Verificar con:

  ```bash
  pip --version
  ```

### 🧱 Instalación

Instala Marimo desde PyPI usando `pip`:

```bash
pip install marimo
```

> 💡 Tip: Si utilizas un entorno virtual (recomendado), primero créalo con:
>
> ```bash
> python -m venv venv
> source venv/bin/activate   # Linux/Mac
> venv\Scripts\activate      # Windows
> ```

### ▶️ Ejecución

Una vez instalado, puedes crear un nuevo notebook y ejecutarlo:

```bash
marimo new mi_notebook.py
```

Esto abrirá el entorno web de Marimo en tu navegador.

Para volver a abrirlo más adelante:

```bash
marimo edit mi_notebook.py
```

### 🧠 Uso básico

* Cada celda de Marimo contiene **Python puro**, sin necesidad de "magic commands".
* Las variables son **reactivas**: si cambias una celda, las dependencias se actualizan automáticamente.
* Puedes exportar el notebook como una **app web interactiva**:

  ```bash
  marimo export mi_notebook.py --html
  ```

### 💡 Ejemplo

```python
import marimo

app = marimo.App()

@app.cell
def x():
    return 10

@app.cell
def y(x):
    return x * 2

@app.cell
def output(y):
    print("El resultado es:", y)

app.run()
```

---

## 🤪 **2. Instalación de Spyder IDE**

### 🧬 ¿Qué es Spyder?

[Spyder](https://www.spyder-ide.org/) (Scientific Python Development Environment) es un **IDE científico** para Python que integra editor de código, consola interactiva, explorador de variables, depurador y soporte para gráficos.
Está especialmente orientado a **científicos, ingenieros y analistas de datos**.

### 🔧 Requisitos previos

* Python 3.8 o superior.
* Se recomienda tener **Anaconda** o **Miniconda**, ya que simplifican la instalación de paquetes científicos.

### 🧱 Instalación con Anaconda (recomendado)

Si tienes **Anaconda** instalado:

```bash
conda install spyder
```

Si usas **Miniconda**, puedes crear un entorno aislado:

```bash
conda create -n spyder-env python=3.11 spyder
conda activate spyder-env
```

### 🧱 Instalación con pip (alternativa)

```bash
pip install spyder
```

Luego ejecuta:

```bash
spyder
```

### ⚙️ Configuración inicial recomendada

1. **Idioma:** Ve a `Tools → Preferences → General → Language` y selecciona “Español”.
2. **Entorno de trabajo:** Desde `View → Panes` activa el “Editor”, “IPython Console” y “Variable Explorer”.
3. **Tema:** Puedes elegir entre modo claro/oscuro (`Tools → Appearance → Theme`).
4. **Linter y formato automático:** Activa `Tools → Preferences → Editor → Code Introspection`.

### 💡 Integración con entornos virtuales

Puedes seleccionar un intérprete Python específico:

1. `Tools → Preferences → Python Interpreter`
2. Selecciona “Use the following Python interpreter”
3. Navega hasta el ejecutable de tu entorno virtual (`.../venv/bin/python` o `.../venv/Scripts/python.exe`)

### 🧠 Uso básico

* **F5** ejecuta el script actual.
* **Ctrl+Enter** ejecuta solo la línea o bloque seleccionado.
* **Variable Explorer** muestra todas las variables activas (como una hoja de cálculo).
* Compatible con librerías como `numpy`, `matplotlib`, `scipy`, `pandas`, `sklearn`, etc.

### 📊 Ejemplo simple

```python
import numpy as np
import matplotlib.pyplot as plt

x = np.linspace(0, 10, 100)
y = np.sin(x)

plt.plot(x, y)
plt.title("Gráfico seno(x)")
plt.xlabel("x")
plt.ylabel("y")
plt.show()
```

Al ejecutarlo en Spyder verás el gráfico integrado en la pestaña “Plots”.

---

## 📚 Comparativa rápida

| Característica           | Marimo 🧬                                  | Spyder 🤪                                                  |
| ------------------------ | ------------------------------------------ | ---------------------------------------------------------- |
| Enfoque principal        | Notebooks reactivas / apps interactivas    | IDE científico tradicional                                 |
| Interfaz                 | Web (moderna)                              | Escritorio (Qt)                                            |
| Requiere instalación GUI | No                                         | Sí                                                         |
| Lenguaje                 | Python puro                                | Python con entorno completo                                |
| Integración científica   | Limitada (aunque creciente)                | Completa (NumPy, SciPy, Matplotlib, etc.)                  |
| Ideal para               | Prototipos interactivos, enseñanza moderna | Análisis científico, ingeniería, programación estructurada |

---

## 🧱 Recomendación final

* Utiliza **Marimo** para crear notebooks interactivos, visuales o proyectos educativos.
* Usa **Spyder** para análisis más extensos, modelado o desarrollo de código científico modular.

Ambos entornos pueden convivir perfectamente y complementarse.

---
